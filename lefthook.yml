# lefthook.yml

pre-commit:
  parallel: true
  commands:
    prettier:
      run: npx prettier --write {staged_files}
      glob: '*.{js,jsx,json,css,md,yml,yaml}'
      stage_fixed: true
    eslint:
      run: npx eslint --fix {staged_files}
      glob: '*.{js,jsx}'
      stage_fixed: true
